#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <assert.h>
#include <inttypes.h>

#include "../../../models/engine/table.h"


select(const char* t_name, const char** c_name, int limit)
{
    for (size_t i = 0; i < )
}